import { login_en as login } from "./module/login";
import { menus_en as menus, topTitle_en as topTitle, allStation_en as allStation,stationType_en as stationType,stationData_en as stationData} from "./module/index";

export default {
  menus,
  topTitle,
  allStation,
  stationType,
  stationData,
  login,
};
